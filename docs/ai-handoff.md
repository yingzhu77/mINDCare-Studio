# AI 交接：{任务名称}

日期：
上一窗口完成：

## 本轮目标

## 相关文件

## 已知风险

## 验证方式

## 未完成项

---

## 进度同步检查清单（完成 Phase 任务后执行）

1. **更新主计划书** `docs/project-fullstack-plan.md`：
   - 更新第 2 节版本号和阶段标记
   - 在前端/后端描述中加入新增功能
   - 将已完成 Phase 从"当前窗口"移入"已完成"列表
   - 补齐"当前遗留问题"（删除已解决的，加入新出现的）
   - 将"下一窗口"重新编号

2. **更新 CLAUDE.md**：
   - 更新 §5 版本号
   - 同步更新 `docs/current-state.md`（版本号、完成记录、遗留问题、不做什么）
   - 如果新增了接口，同步更新 §9 接口列表

3. **验证**：
   - 前端构建：`npm run build`
   - 后端构建：`cd server && npm run build`
   - 测试：`npm run test` + `cd server && npm run test`
