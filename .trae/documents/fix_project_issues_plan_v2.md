# 项目修改计划 (已更新)

## 任务目标
1.  **项目入口调整**：进入项目时首先进入登录页面。
2.  **导航提示优化**：移除菜单栏切换页面时弹出任何无关通知。
3.  **搜索体验改进**：修复知识文章页面搜索表单布局“塌陷”问题，确保搜索后布局稳定。
4.  **接口集成**：将知识文章数据来源更换为指定的 API 接口：`http://159.75.169.224:1235/api`。
5.  **代码质量**：保持代码风格一致，增加详细中文注释。

## 现状分析
1.  **路由系统**：[router/index.js](file:///d:/ai-vue/ai-vue%20project/src/router/index.js) 目前已将根路径 `/` 重定向至登录页。
2.  **导航通知**：用户反馈在菜单切换时会弹出“操作成功”弹窗，虽然目前代码库中未见明显全局钩子，但需深入检查各个视图组件及状态管理。
3.  **搜索布局**：[TableSearch.vue](file:///d:/ai-vue/ai-vue%20project/src/components/TableSearch.vue) 中设置了 `margin-bottom: 0`，在换行显示时会导致表单项重叠。
4.  **数据获取**：[Knowledge.vue](file:///d:/ai-vue/ai-vue%20project/src/views/Knowledge.vue) 使用 `/api` 作为基础路径。根据用户提供的新 URL `http://159.75.169.224:1235/api`，需要同步更新接口请求路径。

## 修改方案

### 1. 路由配置优化 (src/router/index.js)
-   确认根路径 `/` 始终重定向到 `/auth/login`。
-   优化路由守卫，确保跳转流程平滑。

### 2. 彻底移除导航相关的弹窗通知
-   全局搜索并移除任何在页面跳转、组件挂载时触发的 `ElMessage`。
-   确保 `Login.vue` 和 `Register.vue` 中的登录/注册成功提示仅在对应操作发生时显示。
-   排查是否存在冗余的全局 `router.afterEach` 逻辑。

### 3. 修复搜索表单布局 (src/components/TableSearch.vue)
-   修改 `.search-form :deep(.el-form-item)` 样式。
-   将 `margin-bottom: 0` 修改为 `18px`，并添加合适的间距，防止响应式换行时出现重叠。
-   确保搜索后布局不再“收缩”。

### 4. 数据对接新接口 (src/views/Knowledge.vue)
-   更新接口请求配置，确保所有知识文章数据请求都指向 `http://159.75.169.224:1235/api/knowledge/article/page`。
-   如有必要，在 [vite.config.js](file:///d:/ai-vue/ai-vue%20project/vite.config.js) 中更新代理设置以匹配新地址。
-   对返回的数据进行结构校验，确保表格渲染正常。

### 5. 代码优化与注释
-   为所有修改点添加详尽的中文注释。
-   确保代码风格符合现有规范。

## 验证步骤
1.  启动项目，验证自动进入登录页。
2.  在菜单栏连续点击不同菜单项，确认没有任何无关的成功通知弹出。
3.  在知识文章页面搜索框进行输入和查询，确认表单布局稳定，不出现重叠或塌陷。
4.  确认表格数据能够从新接口正常加载。
